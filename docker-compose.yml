version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "7001:80"
    volumes:
      - ./public:/var/www/public
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  php:
    image: php:8.3-fpm
    volumes:
      - ./:/var/www